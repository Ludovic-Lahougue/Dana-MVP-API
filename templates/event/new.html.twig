{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
	{% include "_parts/navbar.html.twig" %}
	<div class="d-flex flex-row h-100">
		<div class="sidebar d-flex flex-column align-items-center me-5">
			<div class="img"></div>
			<h3>Nom structure</h3>
			<div class="align-self-start">
				<h5>Évènements à venir</h5>
				<p>Aucun évènement</p>
			</div>
			<a href="{{ path('app_event_new') }}" class="btn btn-outline-light fs-4">Ajouter un évènement</a>
		</div>
		<div class="ms-5 mt-5">
			<a href="{{ path('app_home') }}" class="btn btn-outline-dark mb-3">Retour</a>
			<div class="card">
				<div class="card-header">
					<h3 class="card-title">Nouvel Évènement</h3>
				</div>
				<div class="card-body">
					{{ include('event/_form.html.twig') }}
				</div>
			</div>
		</div>
	</div>


{% endblock %}
